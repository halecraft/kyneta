# @kyneta/schema-loro — Examples

## [`basic/`](./basic/) — Your First Collaborative Document

Same schema, same API — but backed by Loro CRDTs. Two peers, independent edits, automatic merge.

```sh
bun run example/basic/main.ts
```
