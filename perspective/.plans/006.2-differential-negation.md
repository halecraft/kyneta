# Plan 006.2: Differential Negation and True Incremental Retraction

## Background

Plan 006.1 delivered a unified weighted Datalog evaluator based on DBSP
Z-set weight propagation. The infrastructure is solid: `Relation` stores
integer weights per fact, `Substitution` carries weights through joins
(provenance semiring product), `distinct` clamps via a scoped dirty map,
and `extractDelta` detects zero-crossings without snapshot-and-diff.

However, the evaluator falls back to **wipe-and-recompute** in two cases:

1. **Negation/aggregation strata** — always, even for insertion-only deltas.
2. **Positive strata with retractions** — whenever the input delta contains
   any −1 entry.

The wipe-and-recompute path deletes all derived facts for the stratum,
re-evaluates from scratch, and uses the dirty map to extract the delta.
This is O(|stratum|) per step rather than O(|Δ|). The `retractionsPresent`
flag propagates conservatively through strata: if stratum 0 produces −1
deltas, all higher strata also wipe-and-recompute.

The Z-set abelian group structure supports true incremental retraction
propagation — including through negation. The weight infrastructure is
80% in place. Two capabilities are missing:

1. **Bidirectional delta propagation**: `applyDerivedFact` only seeds the
   next semi-naive iteration for insertions (absent→present). It ignores
   retractions (present→absent), so cascading retractions don't propagate.

2. **Differential negation**: `evaluateNegation` is a boolean filter — it
   passes or blocks substitutions. It cannot produce negative-weight
   substitutions when a previously-absent negated fact appears (blocking
   an existing derivation) or when a previously-present negated fact
   disappears (unblocking a new derivation).

### Key References

- DBSP (Budiu & McSherry, 2023) §3.2 (Z-set joins), §4–5 (nested streams)
- Green et al., 2007 (provenance semirings)
- `theory/incremental.md` §9.3–9.6
- `.plans/006.1-unified-weighted-evaluator.md`
- `LEARNINGS.md` "Retraction Through Negation Strata Still Requires
  Wipe-and-Recompute" — this plan makes that learning obsolete

## Problem Statement

1. **Wipe-and-recompute is O(|stratum|) per step.** For the default rules
   with the native fast path, this is irrelevant (native solvers bypass
   Datalog). For custom Layer 2+ rules and future Layer 3+ user queries,
   negation strata may be large. Wipe-and-recompute becomes a bottleneck
   that scales with total derived facts, not with the delta size.

2. **Positive strata with retractions fall back unnecessarily.** The weight
   infrastructure already supports −1 propagation through joins. The only
   gap is that `applyDerivedFact` doesn't feed retractions into the
   semi-naive loop and `evaluatePositiveAtom` filters out negative-weight
   entries from the delta.

3. **Dead code.** `wipeAndRecompute` (~90 LOC), `evaluateNegationStratum`
   (~40 LOC, already unreachable), the `retractionsPresent` flag threading,
   the `inputHasRetractions` parameter, `diffDerivedSnapshots` (used only
   for rule changes), and `hasNegOrAgg` routing in `evaluateStratumFromDelta`
   are all artifacts of the wipe-and-recompute approach.

4. **The evaluator's public contract is misleading.** The test suite
   includes a section titled "retraction through negation without DRed" but
   the underlying implementation **is** DRed (wipe-and-recompute). The tests
   verify outcomes, not mechanisms.

## Success Criteria

1. `evaluateStratumFromDelta` is a single algorithm — no conditional
   routing based on `hasNegOrAgg` or `inputHasRetractions`. The same
   weighted semi-naive loop handles positive strata, negation strata,
   insertions, and retractions uniformly.

2. For positive strata with retractions: O(|Δ|×|DB|) per step, not
   O(|stratum|). Retractions cascade through recursive joins via the
   semi-naive loop.

3. For negation strata: O(|Δ_negated|×|DB_positive| + |Δ_positive|×|DB|)
   per step. Differential negation processes only the changed entries in
   the negated relation, not the entire relation.

4. `wipeAndRecompute`, `evaluateNegationStratum`, and all conditional
   routing code are deleted.

5. All existing tests pass. The three-way oracle (batch ≡ single-step ≡
   one-at-a-time) continues to hold. Retraction tests verify outcomes
   that are now achieved via true weight propagation rather than
   wipe-and-recompute.

6. New tests verify the mechanism: intermediate weight states, cascading
   retractions through recursive rules, and differential negation through
   stratified negation.

7. Aggregation strata continue to work correctly. Aggregation is a
   group-by boundary that resets provenance, so its incremental story is
   different from negation. This plan handles aggregation strata via a
   targeted re-evaluation of affected groups (not full wipe), or retains
   wipe-and-recompute scoped to aggregation-only strata as a documented
   limitation.

## Gap Analysis

### What Exists

- **Weight-carrying `Substitution`**: `{ bindings, weight }` flows through
  all evaluation functions.
- **Provenance product in joins**: `evaluatePositiveAtom` multiplies
  `sub.weight × tuple.weight`.
- **Z-set addition in `groundHead`**: Duplicate facts sum weights.
- **Dirty-map `distinct`**: `applyDistinct` clamps to 0/1 on modified
  entries only.
- **Zero-crossing delta extraction**: `extractDelta` compares `preWeight`
  to current weight, emitting +1 or −1.
- **`Relation.addWeighted`**: Handles negative weights correctly (stores,
  sums, prunes at zero).
- **`Relation.allWeightedTuples`**: Returns all entries including negative
  weights (for delta databases).
- **Inter-stratum delta propagation**: `step()` already merges stratum
  output deltas into `currentInputDelta` for higher strata.

### What's Missing

1. **`evaluatePositiveAtom` uses `weightedTuples()`** (weight > 0 only).
   When matching against a delta database, −1 entries are invisible. Must
   use `allWeightedTuples()` so that retracted delta entries produce
   negative-weight substitutions via the provenance product.

2. **`applyDerivedFact` only propagates insertions.** When a fact crosses
   from present to absent (weight > 0 → ≤ 0), the retraction is not added
   to `nextDelta`. Cascading retractions in recursive rules stall.

3. **Semi-naive convergence check uses `currentDelta.size`** (weight > 0
   count). A delta containing only −1 entries has `size === 0` and
   terminates the loop prematurely. Must use a check that includes
   negative-weight entries.

4. **No `evaluateDifferentialNegation` function.** When the negated
   relation changes, existing bindings from positive atoms must be
   re-evaluated: newly-present negated facts block (+1 in negated →
   −1 in output), newly-absent negated facts unblock (−1 in negated →
   +1 in output).

5. **No `evaluateRuleDifferentialNegation` function.** Mirrors
   `evaluateRuleSemiNaive` but the delta source is a negation body
   element, not a positive atom.

6. **No `getNegationAtomIndices` helper.** Needed to enumerate negation
   body elements for differential processing per rule.

7. **Routing logic in `evaluateStratumFromDelta`** dispatches to
   `wipeAndRecompute` based on boolean flags. Must be replaced with the
   unified loop.

8. **`step()` threads `retractionsPresent` flag** through strata. Must be
   removed — each stratum handles its own retractions via the unified loop.

## Architecture

### Unified Semi-Naive Loop

Every stratum — positive-only, negation, or mixed — uses the same loop:

```
evaluateStratumFromDelta(rules, db, inputDelta):
  dirty = new DirtyMap

  // Seed: for each rule, evaluate semi-naively from inputDelta.
  currentDelta = seed(rules, db, inputDelta, dirty)
  applyDistinct(db, dirty)

  // Iterate until convergence.
  while currentDelta has entries:
    nextDelta = empty
    for rule in rules:
      // Positive atom delta sources (standard semi-naive)
      for deltaIdx in positiveAtomIndices(rule):
        derived = evaluateRuleSemiNaive(rule, db, currentDelta, deltaIdx)
        for wf in derived: applyDerivedFact(wf, db, dirty, nextDelta)

      // Negation atom delta sources (differential negation)
      for negIdx in negationAtomIndices(rule):
        if currentDelta has entries for negated predicate at negIdx:
          derived = evaluateRuleDiffNeg(rule, db, currentDelta, negIdx)
          for wf in derived: applyDerivedFact(wf, db, dirty, nextDelta)

    applyDistinct(db, dirty)
    currentDelta = nextDelta

  return extractDelta(db, dirty)
```

### Differential Negation

`evaluateDifferentialNegation(atom, delta, subs)` matches the negation
atom against the **delta** (not the full DB). For each matching entry
in the delta:

- If delta weight is +1 (fact newly appeared in negated relation): this
  binding is now blocked → emit substitution with `weight *= -1`.
- If delta weight is −1 (fact disappeared from negated relation): this
  binding is now unblocked → emit substitution with `weight *= +1`.

In both cases the formula is: `output_weight = sub.weight × (-deltaWeight)`.

The sign inversion encodes the semantics of negation-as-failure:
appearance of a negated fact *removes* derivations; disappearance of a
negated fact *adds* derivations.

`evaluateRuleDifferentialNegation(rule, fullDb, delta, negIdx)` mirrors
`evaluateRuleSemiNaive`. All body elements except the one at `negIdx` are
evaluated against `fullDb`. The element at `negIdx` (a negation) is
evaluated differentially against `delta`. Other negation body elements
in the same rule (if any) are evaluated normally against `fullDb` (boolean
filter).

### Bidirectional `applyDerivedFact`

The current function detects absent→present crossings and seeds
`nextDelta` with +1. The fix adds present→absent detection: when a
fact's weight crosses from > 0 to ≤ 0, seed `nextDelta` with −1.
This allows cascading retractions through recursive joins (e.g.,
transitive closure losing an edge).

### `evaluatePositiveAtom` Change

Switch from `relation.weightedTuples()` to `relation.allWeightedTuples()`.
In the accumulated DB (post-`distinct`), all entries have weight 0 or 1
and zero-weight entries are pruned, so `allWeightedTuples()` returns
exactly the same set as `weightedTuples()`. In a delta DB, it correctly
includes −1 entries. No behavioral change for batch evaluation or
insertion-only incremental evaluation.

### Convergence Check

Replace `currentDelta.size > 0` with a check that accounts for
negative-weight entries. Options: `currentDelta.allEntryCount > 0`, or
add a `Database.hasAnyEntries()` method. The latter avoids exposing the
somewhat confusing `allEntryCount` name in a loop condition.

### Aggregation Strata — Scoped Limitation

Aggregation (`min`, `max`, `count`, `sum`) is a group-by boundary that
resets provenance to weight 1. Differential aggregation would require
maintaining per-group state to detect when a group's aggregate value
changes — essentially a mini-evaluator per group. This is substantially
more complex than differential negation and is not needed for the default
rules (no aggregation in default LWW or Fugue rules).

This plan **retains wipe-and-recompute for aggregation-only strata** as
a scoped, documented limitation. The routing for this is minimal: check
if a stratum has aggregation body elements. If yes, wipe-and-recompute.
If the stratum has negation but no aggregation, use the unified loop.
This is a much narrower condition than the current "any negation or
aggregation" check.

The wipe-and-recompute code is retained but renamed to
`recomputeAggregationStratum` and scoped specifically to aggregation.
The generic `wipeAndRecompute` function is deleted.

### Convergence Argument

In stratified Datalog:

- **Lower-stratum predicates are converged** before the current stratum
  begins. The differential negation pass fires only for entries in the
  input delta whose predicates match negation atoms — these are from
  lower strata and don't change during iteration.
- **Intra-stratum iteration** involves only the stratum's own derived
  predicates, which don't appear in its own negation atoms (cyclic
  negation is rejected by stratification).
- **Positive sub-loop convergence**: Each +1 adds a new fact (bounded
  by finite DB). Each −1 retracts a fact that was at weight 1; after
  `distinct` clamping, the number of facts with weight > 0 strictly
  decreases or stays the same. No infinite oscillation is possible.
- **Recursive rules with retractions** (e.g., transitive closure losing
  an edge): The −1 propagates through the join. Each iteration can
  retract at most as many facts as currently have weight > 0. The total
  set of weight > 0 facts decreases monotonically per iteration.
  Convergence follows from the finite domain.

### Functional Core / Imperative Shell

The split is preserved from Plan 006.1:

- **Functional core** (`evaluateStratumFromDelta`): Given rules, a mutable
  db, and an input delta, run the unified semi-naive loop to convergence
  and return the output delta. Independently testable.
- **Imperative shell** (`createEvaluator` + `step`): Manages accumulated
  state, affected-strata computation, inter-stratum delta propagation.
  Simplified by removing `retractionsPresent` flag threading.

## Phases and Tasks

### Phase 1: Differential Negation Primitives 🔴

Add the new evaluation functions in `evaluate.ts` alongside the existing
`evaluatePositiveAtom`, `evaluateNegation`, etc.

#### Tasks

- **1.1** Add `getNegationAtomIndices(body)` to `evaluate.ts`. Returns
  indices of `kind: 'negation'` body elements. Mirrors
  `getPositiveAtomIndices`. 🔴

- **1.2** Add `evaluateDifferentialNegation(atom, delta, subs)` to
  `evaluate.ts`. Matches the negation atom against `delta` using
  `allWeightedTuples()`. For each match, emits a substitution with
  `weight = sub.weight × (-deltaWeight)`. 🔴

- **1.3** Add `evaluateRuleDifferentialNegation(rule, fullDb, delta, negIdx)`
  to `evaluate.ts`. Mirrors `evaluateRuleSemiNaive`: evaluates all body
  elements against `fullDb` except the negation at `negIdx`, which uses
  `evaluateDifferentialNegation` against `delta`. Returns
  `WeightedFact[]`. 🔴

- **1.4** Change `evaluatePositiveAtom` to use `allWeightedTuples()` instead
  of `weightedTuples()`. Verify no behavioral change for batch evaluation
  (all accumulated DB entries have weight 0 or 1; zero-weight entries are
  pruned). 🔴

- **1.5** Export new functions from `datalog/index.ts`. 🔴

#### Tests

- Unit test `evaluateDifferentialNegation`: hand-crafted substitutions
  against a delta with +1 and −1 entries; verify output weights.
- Unit test `evaluateRuleDifferentialNegation` with the `winner` rule:
  `active_value` in fullDb, `superseded` in delta; verify correct +1/−1
  weighted facts.
- Regression test: `evaluatePositiveAtom` with `allWeightedTuples()` on a
  fully converged DB (all weights 1) produces identical results to before.

### Phase 2: Unified Semi-Naive Loop 🔴

Replace the conditional routing in `evaluateStratumFromDelta` with the
unified loop that handles both positive and negation atoms as delta
sources.

#### Tasks

- **2.1** Modify `applyDerivedFact` to propagate both directions: add
  retractions (present→absent zero-crossings) to `nextDelta` with weight
  −1 via `addWeightedFact`. 🔴

- **2.2** Add `Database.hasAnyEntries(): boolean` method that returns true
  if any relation has any stored entries (regardless of weight sign).
  Replace `currentDelta.size > 0` convergence check in the semi-naive
  loop. 🔴

- **2.3** Rewrite `evaluateStratumFromDelta` as the unified loop. Remove
  `hasNegOrAgg` and `inputHasRetractions` parameters. The function
  signature becomes
  `evaluateStratumFromDelta(rules, db, inputDelta): Database`.
  The single algorithm: seed from `inputDelta` via semi-naive (positive
  atoms) and differential negation (negation atoms), then iterate with
  both delta sources until convergence. 🔴

- **2.4** Special-case aggregation: if the stratum has aggregation body
  elements, delegate to `recomputeAggregationStratum` (the extracted
  wipe-and-recompute logic, scoped to aggregation). This is the only
  conditional branch. 🔴

- **2.5** Delete `wipeAndRecompute`. Rename/extract the aggregation-only
  recompute path as `recomputeAggregationStratum`. Delete
  `evaluateNegationStratum` (already dead code). 🔴

#### Tests

- Unit test: `evaluateStratumFromDelta` with positive rules and −1 input
  delta (retraction cascades through transitive closure).
- Unit test: `evaluateStratumFromDelta` with negation rules and +1 input
  delta to a negated predicate (newly blocked derivation produces −1
  output).
- Unit test: `evaluateStratumFromDelta` with negation rules and −1 input
  delta to a negated predicate (newly unblocked derivation produces +1
  output).
- Unit test: aggregation stratum still wipe-and-recomputes correctly.
- Convergence test: a recursive rule (transitive closure) with edge
  retraction converges and produces correct delta.

### Phase 3: Simplify `step()` and Wire-Up 🔴

Remove the `retractionsPresent` flag threading from `step()` and simplify
the stratum evaluation loop.

#### Tasks

- **3.1** Remove `retractionsPresent` flag and `hasRetractionsInDelta`
  detection from `step()`. Every stratum call becomes:
  `evaluateStratumFromDelta(stratum.rules, db, currentInputDelta)`. 🔴

- **3.2** Update the rule-change path. Currently it wipes all derived facts
  and replays all strata, then diffs snapshots via `diffDerivedSnapshots`.
  After this change, the replay path can use
  `evaluateStratumFromDelta(rules, db, fullInputDelta)` for each stratum,
  where `fullInputDelta` contains all ground facts as +1 (same as batch
  mode). Delta extraction via the dirty map eliminates the need for
  explicit snapshot-and-diff on this path too. Remove
  `diffDerivedSnapshots`. 🔴

- **3.3** Remove `hasNegOrAgg` from all call sites in `step()`. The
  stratum-level function determines aggregation internally. 🔴

#### Tests

- All existing `createEvaluator` tests pass unchanged.
- Three-way oracle (batch ≡ single-step ≡ one-at-a-time) continues to hold
  for all existing test scenarios.
- Retraction tests now exercise true weight propagation (verify by
  confirming `wipeAndRecompute` is deleted).

### Phase 4: Verification and Cleanup 🔴

Comprehensive verification, dead code removal, documentation updates.

#### Tasks

- **4.1** Run full test suite. Verify all 1261+ tests pass. 🔴

- **4.2** Add targeted mechanism-verification tests: 🔴
  - LWW retraction: insert two competing values, retract the winner.
    Verify the intermediate weight state (superseded facts retracted,
    winner fact retracted, new winner derived) without wipe-and-recompute.
  - Fugue retraction (via Datalog, not native path): add three seq
    elements, retract the middle one's structure constraint. Verify
    `fugue_before` pairs are correctly updated via differential negation.
  - Recursive retraction cascade: `reachable(X,Z) :- edge(X,Y),
    reachable(Y,Z)`. Insert edges a→b→c→d, then retract b→c. Verify
    `reachable(a,c)`, `reachable(a,d)`, `reachable(b,d)` are all
    retracted.
  - Retraction with alternative support: `reachable(a,c)` has two paths
    (a→b→c and a→e→c). Retract a→b. Verify `reachable(a,c)` survives
    (weight 2 → 1, distinct keeps it at 1).
  - Differential negation timing: `winner` rule with negation over
    `superseded`. Insert a new `active_value` that supersedes the
    current winner. Verify −1 for old winner and +1 for new winner
    appear in the stratum's output delta.

- **4.3** Remove dead code: 🔴
  - `wipeAndRecompute` function
  - `evaluateNegationStratum` function
  - `diffDerivedSnapshots` function (if eliminated in Phase 3)
  - `stratumHasNegationOrAggregation` (replaced by aggregation-only check)
  - `hasNegOrAgg` local variables and parameter
  - `retractionsPresent` / `hasRetractionsInDelta` in `step()`
  - `inputHasRetractions` parameter on `evaluateStratumFromDelta`

- **4.4** Update `TECHNICAL.md`: 🔴
  - Datalog Evaluator section: remove "Negation/aggregation strata always
    use wipe-and-recompute" language; describe the unified semi-naive loop
    with differential negation.
  - Incremental Pipeline section: remove "Wipe-and-recompute is used when
    retractions are present" caveat.
  - Note aggregation strata as the remaining scoped limitation.

- **4.5** Update `LEARNINGS.md`: 🔴
  - Add learning: "Differential Negation Is the DBSP Complement to
    Weighted Joins" documenting the `weight = sub.weight × (-deltaWeight)`
    formula and why it works.
  - Add learning: "allWeightedTuples() Is the Correct Default for
    Delta-Aware Evaluation" documenting why the weight > 0 filter in
    `weightedTuples()` is only correct for the accumulated DB, not for
    delta sources.
  - Update "Retraction Through Negation Strata Still Requires
    Wipe-and-Recompute" to note it is now obsolete.

- **4.6** Update `theory/incremental.md` §9.4: remove the "current
  limitation" paragraph about negation strata and wipe-and-recompute. 🔴

- **4.7** Update `.plans/006.1-unified-weighted-evaluator.md`: mark the
  negation/retraction limitation as resolved by Plan 006.2. 🔴

## Transitive Effect Analysis

### `evaluatePositiveAtom` → `allWeightedTuples()`

This change affects every call to `evaluatePositiveAtom`, which is called
from `evaluateRule`, `evaluateRuleSemiNaive`, and the new
`evaluateRuleDifferentialNegation`.

**Accumulated DB (post-`distinct`):** All entries have weight ∈ {0, 1}.
Zero-weight entries are pruned by `addWeighted`. So `allWeightedTuples()`
returns exactly the entries that `weightedTuples()` returns. No behavioral
change.

**Mid-iteration DB (pre-`distinct`):** Entries may have weight > 1 or
weight < 0. Using `allWeightedTuples()` exposes these to
`evaluatePositiveAtom`. Weight > 1 entries produce weight > 1
substitutions, which get summed in `groundHead` and then clamped by
`distinct`. Weight < 0 entries produce negative substitutions, which
correctly represent retracted derivations. This is the intended behavior.

**Batch mode:** All ground facts have weight 1. Derived facts accumulate
weight 1 (clamped by `distinct`). `allWeightedTuples()` returns exactly
the same entries as `weightedTuples()`. Zero behavioral change.

**Tests affected:** None — the output is identical for all existing code
paths. New behavior only manifests when delta databases contain −1 entries,
which is tested by new tests.

### `applyDerivedFact` bidirectional propagation

Previously, only absent→present crossings seeded `nextDelta`. Now
present→absent crossings also seed `nextDelta` with −1.

**Risk:** A retraction could cascade incorrectly if `distinct` doesn't
clamp properly. Mitigation: `applyDistinct` runs after each iteration,
clamping negative weights to 0. A fact that drops to weight −1 during
iteration is clamped to 0, and `extractDelta` sees a zero-crossing only
if `preWeight` was > 0.

**Risk:** Infinite oscillation between +1 and −1. This cannot happen
because: (a) `distinct` clamps negative to 0 and supernumerary to 1,
so each iteration starts from a clean 0/1 state; (b) the delta for
each iteration is strictly derived from the previous delta via rule
evaluation, and the set of derivable facts is finite.

### `evaluateStratumFromDelta` signature change

The `hasNegOrAgg` and `inputHasRetractions` parameters are removed.

**Call sites in `evaluator.ts`:** All calls in `step()` must be updated.
There are approximately 4 call sites: the main incremental loop, the
first-step-with-no-facts path, the rule-change replay path, and the
batch wrapper's internal path via `evaluatePositiveStratum`.

**No external callers:** `evaluateStratumFromDelta` is exported but
the only external caller is the test suite, which will be updated in
the same phase.

### Removal of `retractionsPresent` flag in `step()`

The flag currently forces higher strata to wipe-and-recompute when lower
strata produce −1 deltas. Removing it means higher strata process the
−1 deltas via the unified loop. This is the intended behavior — the
unified loop handles retractions.

**Risk:** If the unified loop is incorrect, stale derived facts could
remain. Mitigation: the three-way oracle test catches any divergence
between batch (fresh evaluator) and incremental (accumulated state).

### Aggregation strata retained as wipe-and-recompute

Strata with aggregation body elements continue to wipe-and-recompute.
The check narrows from `hasNegOrAgg` to `hasAggregation`. Strata with
negation but no aggregation now use the unified loop.

**Default rules:** No aggregation in LWW or Fugue rules. Unaffected.

**User-defined rules with aggregation:** Continue to work correctly via
wipe-and-recompute. Future plan can address differential aggregation.

### `Database.hasAnyEntries()` addition

New method on `Database`. No existing method changes. Used only in the
semi-naive convergence check. No transitive effects.

### Resolution extraction

`winnerFactsToResolution` and `fuguePairFactsToResolution` read from the
output delta `Database` using `allWeightedTuples()`. These are unchanged
— the output delta is produced by `extractDelta`, which emits +1 and −1
entries. The resolution extraction logic is already correct for both signs.

### `evaluate.ts` exports

New exports: `getNegationAtomIndices`, `evaluateDifferentialNegation`,
`evaluateRuleDifferentialNegation`. Added to `datalog/index.ts`. No
existing exports removed.

## Testing Strategy

### Three-Way Oracle (existing, unchanged)

All three paths must produce the same database:
1. `evaluate(rules, allFacts)` — batch
2. `createEvaluator(rules) + step(allFacts)` — single step
3. `createEvaluator(rules) + step(fact₁) + ... + step(factₙ)` — one at a time

### Retraction Cascade Unit Tests (new)

Targeted tests for the unified loop with small rule sets:
- Transitive closure with edge retraction
- Transitive closure with alternative support (weight stays 1)
- Diamond-shaped dependency (fact derivable via two paths, one retracted)

### Differential Negation Unit Tests (new)

- `evaluateDifferentialNegation` with hand-crafted subs and delta
- `evaluateRuleDifferentialNegation` with the `winner` rule
- Full stratum evaluation: `superseded` +1 in delta → `winner` −1 in output
- Full stratum evaluation: `superseded` −1 in delta → `winner` +1 in output

### Mechanism Verification Tests (new)

Tests that verify the evaluator does NOT wipe-and-recompute:
- After Phase 4, `wipeAndRecompute` is deleted, so any test that retracts
  and verifies correct results is implicitly proving the mechanism.
- Explicit weight-state tests: after feeding a −1 delta through a positive
  stratum, verify the dirty map only contains entries for affected facts
  (not all derived facts).

### Permutation Tests (existing, unchanged)

All orderings of 3 values produce the same resolution. These tests
exercise both insertion and supersession paths.

## Resources for Implementation

### Files to Read

- `src/datalog/evaluate.ts` — `evaluateRule`, `evaluateRuleSemiNaive`,
  `evaluatePositiveAtom`, `evaluateNegation`, `getPositiveAtomIndices`,
  `groundHead`
- `src/datalog/evaluator.ts` — `evaluateStratumFromDelta`,
  `wipeAndRecompute`, `evaluatePositiveStratum`, `evaluateNegationStratum`,
  `applyDerivedFact`, `applyDistinct`, `extractDelta`, `touchFact`,
  `createEvaluator` + `step()`, `diffDerivedSnapshots`
- `src/datalog/types.ts` — `Relation` (`weightedTuples`,
  `allWeightedTuples`, `addWeighted`, `size`, `allEntryCount`), `Database`
- `src/datalog/unify.ts` — `matchAtomWithTuple`, `matchAtomAgainstRelation`
- `src/datalog/stratify.ts` — `Stratum`, `bodyPredicates`, `headPredicates`
- `src/datalog/index.ts` — barrel exports
- `src/bootstrap.ts` — `buildDefaultLWWRules`, `buildDefaultFugueRules`
  (for understanding rule structure)
- `tests/datalog/evaluator.test.ts` — existing test patterns and helpers
- `tests/datalog/incremental-evaluate.test.ts` — existing retraction tests
- `theory/incremental.md` §9.3–9.6
- `LEARNINGS.md` "Retraction Through Negation Strata Still Requires
  Wipe-and-Recompute"

### Files to Modify

- `src/datalog/evaluate.ts` — add `getNegationAtomIndices`,
  `evaluateDifferentialNegation`, `evaluateRuleDifferentialNegation`;
  change `evaluatePositiveAtom` to use `allWeightedTuples()`
- `src/datalog/evaluator.ts` — rewrite `evaluateStratumFromDelta`;
  delete `wipeAndRecompute`, `evaluateNegationStratum`; modify
  `applyDerivedFact`; simplify `step()`; remove `diffDerivedSnapshots`
  (if rule-change path is unified); extract
  `recomputeAggregationStratum`; add `stratumHasAggregation` helper
- `src/datalog/types.ts` — add `Database.hasAnyEntries()`
- `src/datalog/index.ts` — export new functions
- `TECHNICAL.md` — update Datalog Evaluator and Incremental Pipeline
  sections
- `LEARNINGS.md` — add new learnings, mark old one obsolete
- `theory/incremental.md` §9.4 — remove limitation paragraph
- `.plans/006.1-unified-weighted-evaluator.md` — mark resolved

### Test Files to Modify

- `tests/datalog/evaluator.test.ts` — add differential negation tests,
  retraction cascade tests, mechanism verification tests; remove references
  to "without DRed" (it's now the only path)

## Alternatives Considered

### Maintain `wipeAndRecompute` for Negation, Fix Only Positive Strata

Fix `applyDerivedFact` and `evaluatePositiveAtom` to handle retractions
in positive strata, but keep wipe-and-recompute for negation strata.
Rejected because: (a) the differential negation implementation is
straightforward once positive retraction propagation works, (b) negation
strata are exactly where the performance benefit matters most (the LWW
winner rule is in a negation stratum), (c) keeping two paths perpetuates
the conditional routing complexity.

### Full Differential Aggregation

Implement incremental aggregation by tracking per-group state and
detecting when aggregate values change. Rejected for this plan because:
(a) no default rules use aggregation, (b) differential aggregation
requires maintaining group membership sets, which is substantially more
complex, (c) the aggregation wipe-and-recompute is bounded by the size
of the aggregation stratum, which is typically small, (d) it can be
addressed in a future plan when Layer 3+ queries create large aggregation
strata.

### Per-Fact Provenance DAGs Instead of Z-Set Weights

Track which specific ground facts support each derivation, enabling
precise retraction by walking the DAG. Rejected because: (a) DBSP's
Z-set arithmetic subsumes this via weight counting, (b) per-fact DAGs
are O(|derivation paths|) in memory, which can be exponential for
recursive rules, (c) `distinct` keeps weights at 0/1, making the weight
approach O(1) per fact, (d) the weight infrastructure is already built.

### Treat Negation Body Elements as Anti-Joins with Z-Set Semantics

Model `not P(X)` as a Z-set anti-join: `subs ⋈ (1 - distinct(P))`.
This is mathematically clean but requires materializing `1 - distinct(P)`
for the negated relation, which is the complement set — potentially
infinite or at least very large. Rejected in favor of the targeted
differential approach which only processes the *changed* entries in the
negated relation.

### Defer to Plan 008 (Query Layer)

Address differential negation when user queries create large negation
strata. Rejected because: (a) the fix is localized to two files and
~150 lines of new code, (b) it eliminates ~200 lines of conditional
routing and wipe-and-recompute code, (c) it makes the evaluator
conceptually simpler (one algorithm, not three paths), (d) Plan 008
would inherit the complexity if deferred.