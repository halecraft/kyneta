# Plan 006.1: Unified Weighted Datalog Evaluator

## Background

Plan 006 delivered an end-to-end incremental pipeline. The Datalog evaluator
was made incremental via two strategies: semi-naive delta propagation for
monotone strata (no retractions), and DRed (delete-and-rederive) for negation
strata and any stratum when retractions are present. DRed wipes all derived
facts for a stratum and recomputes from scratch — O(|stratum|) per step rather
than O(|Δ|). A snapshot-and-diff at the boundary recovers the output delta.

This was a pragmatic choice. The batch evaluator had no weight infrastructure,
provenance tracking was complex, and DRed's cost was bounded by slot/parent
count for the default rules. But it left the Datalog evaluator as the only
stage in the pipeline that falls back to recomputation rather than computing
deltas from deltas. It also left two separate evaluator implementations
(batch in `evaluate.ts`, incremental in `incremental-evaluate.ts`) sharing
inner functions but duplicating stratum-level logic in four places:
`evaluatePositiveStratum` and `evaluateStratumWithNegation` (batch),
`evaluateMonotoneStratumIncremental` and `evaluateStratumDRed`
(incremental), plus `fullRecompute` (rule-change recovery, which is
itself a near-copy of the batch path).

DBSP (Budiu & McSherry, 2023) provides the principled alternative: Z-set
weights on relations. A derived fact's weight is the product of its input
weights. When an input is retracted (weight −1), the retraction propagates
through joins automatically. `distinct` (clamp to 0/1) after each semi-naive
iteration prevents weight explosion from transitive closure. The same
algorithm handles monotone and negation strata — no DRed, no snapshot-and-diff,
no special cases.

### Key Specification References

- theory/incremental.md §9.3 (cross-time incrementality)
- theory/incremental.md §9.4 (the provenance requirement)
- theory/incremental.md §9.5 (monotone strata)
- theory/incremental.md §9.6 (stratified negation strata)
- DBSP (Budiu & McSherry, 2023) §3.2 (Z-set joins), §4–5 (nested streams)
- Green et al., 2007 (provenance semirings)

## Problem Statement

1. **Two evaluators.** `evaluate.ts` (batch, stateless) and
   `incremental-evaluate.ts` (incremental, stateful) implement the same
   algorithm with duplicated stratum-level logic. `fullRecompute()` in the
   incremental evaluator is a copy-paste of the batch evaluator. Bug fixes
   or enhancements must be applied in multiple places.

2. **DRed is not incremental.** For negation strata, the evaluator wipes all
   derived facts and recomputes from scratch. For monotone strata with
   retractions, it does the same. This is O(|stratum|) per step, not O(|Δ|).

3. **Snapshot-and-diff is redundant work.** Before each step, the evaluator
   clones all derived predicates. After the step, it diffs against the clone.
   This is O(|derived facts|) per step — work that exists solely because the
   evaluator doesn't produce deltas natively.

4. **`evaluateMonotoneStratumIncremental` ignores its `_inputDelta`
   parameter.** The initial pass evaluates all rules against the full DB
   rather than seeding semi-naive from the delta. The "incremental" monotone
   path is O(|DB|²), not O(|Δ|×|DB|).

5. **Dead code.** `isReferencedByStratum`, `diffFuguePairs`, `flattenPairs`
   are defined but never called. `snapshotDerivedPredicates` and
   `diffDatabases` exist only to support the snapshot-and-diff pattern.

## Success Criteria

1. One evaluator implementation. `evaluate(rules, facts)` is a convenience
   wrapper over `createEvaluator(rules).step(allFactsAsPositive)`. No
   duplicated stratum-level logic.

2. `Relation` and `Database` support integer weights per fact. Ground facts
   enter with weight ±1. Derived facts accumulate weights from joins.
   `distinct` clamps to 0/1 after each semi-naive iteration.

3. The semi-naive loop propagates both +1 and −1 deltas through joins and
   negation. No DRed. No snapshot-and-diff. The output delta falls out of
   the Z-set arithmetic.

4. Monotone strata are seeded from the input delta (the `_inputDelta` fix).
   Negation strata use the same weighted semi-naive — the algorithm is
   uniform.

5. All 1219 existing tests pass. The batch pipeline (`solve()`) continues to
   serve as the differential testing oracle — it calls through to the same
   unified evaluator.

6. Dead code removed (see Migration Inventory below for the full list).

## Gap Analysis

### What Exists

- **`Relation`** — `Map<string, FactTuple>`. Boolean presence. `add`,
  `has`, `remove`, `tuples`, `union`, `difference`, `clone`.
- **`Database`** — `Map<string, Relation>`. `addFact`, `removeFact`,
  `hasFact`, `mergeFrom`, `clone`, `size`.
- **`Substitution`** — `ReadonlyMap<string, Value>`. No weight. Used
  throughout `unify.ts`, `evaluate.ts`, `aggregate.ts`.
- **`evaluateRule`** / **`evaluateRuleSemiNaive`** — take `Database`
  params, return `Fact[]`. No weights.
- **`evaluatePositiveAtom`** — iterates `relation.tuples()`, calls
  `matchAtomAgainstRelation`. Returns `Substitution[]`.
- **`evaluateNegation`** — filters substitutions where negated atom has
  no match. Returns `Substitution[]`.
- **`groundHead`** — produces `Fact[]` from substitutions. Deduplicates.
- **`evaluate()`** — stateless batch entry point. Stratifies, evaluates
  each stratum, returns `Database`.
- **`createIncrementalDatalogEvaluator()`** — stateful. Maintains
  persistent `Database`. DRed for negation strata. Snapshot-and-diff for
  output deltas.
- **`ZSet<T>`** — `ReadonlyMap<string, ZSetEntry<T>>`. Full algebra in
  `base/zset.ts`. Used at pipeline boundaries.

### What's Missing

1. **Weighted `Relation`** — `Map<string, { tuple: FactTuple, weight: number }>`.
   `addWeighted(tuple, weight)` sums weights. `getWeight(tuple)` returns
   weight. `tuples()` returns weight > 0 tuples (backward compatible).
   Zero-weight entries pruned.
2. **Weighted `Database`** — `addWeightedFact(f, weight)`. Ground facts
   enter with weight ±1.
3. **Weighted `Substitution`** — redefine `Substitution` from
   `ReadonlyMap<string, Value>` to
   `{ bindings: ReadonlyMap<string, Value>, weight: number }`.
   One type, one name — not a new type alongside the old one. Positive
   atom join: weight = sub.weight × tuple.weight. Negation: weight
   preserved (filter). Guard: weight preserved (filter).
4. **Weighted rule evaluation** — `evaluateRule` takes and produces
   `Substitution`s (now carrying weights). `groundHead` produces
   `{ fact: Fact, weight: number }[]`. Multiple derivations of the same
   fact sum weights. No parallel unweighted variants — one set of
   functions, weights always present.
5. **Unified evaluator** — one `createEvaluator(rules)` that subsumes both
   batch and incremental paths. `step(delta)` is the general case.
   `evaluate(rules, facts)` is a convenience wrapper.
6. **Scoped `distinct`** — after each semi-naive iteration, clamp weights
   to 0/1 only for entries modified during that iteration (tracked via a
   dirty set). O(|modified|) per iteration, not O(|relation|).

## Architecture

### Weight Propagation Through the Evaluation Core

The fundamental change: `Relation` stores `{ tuple, weight }` per entry.
Rule evaluation threads weights through substitutions. The semi-naive loop
propagates weighted deltas.

`Substitution` is redefined from a raw Map to a struct:

```typescript
// Before (types.ts):
type Substitution = ReadonlyMap<string, Value>;

// After (types.ts):
interface Substitution {
  readonly bindings: ReadonlyMap<string, Value>;
  readonly weight: number;
}
```

No new type name — `Substitution` remains `Substitution`. The access
pattern changes: `sub.get('X')` → `sub.bindings.get('X')`. All evaluation
functions — in `evaluate.ts`, `unify.ts`, and `aggregate.ts` — operate on
the enriched `Substitution`. Functions that don't inspect the weight
(matching, guards) pass it through unchanged. Functions that need it
(positive atom join, groundHead) read/multiply it. `EMPTY_SUBSTITUTION`
becomes `{ bindings: new Map(), weight: 1 }`.

**Positive atom join:** When a `Substitution` matches a tuple with weight
`w`, the extended substitution's weight is `sub.weight × w`. This is the
provenance semiring product.

**Negation:** A boolean filter. If the negated predicate has a matching fact
with weight > 0 (after `distinct`), the substitution is filtered out
(dropped). Otherwise it passes through with weight preserved. Negation
does not produce fractional weights.

**Guard:** A boolean filter. Weight preserved on pass, substitution dropped
on fail.

**Aggregation:** For `count`, each contributing fact's weight matters. For
`min`/`max`/`sum`, facts with weight > 0 participate. Deferred: our default
rules don't use aggregation in the incremental path, and aggregation's
weight semantics need careful per-function treatment. Aggregation continues
to use the existing boolean semantics (facts with weight > 0 are "present").
The output substitution's weight is set to 1 (aggregation is a group-by
boundary that resets provenance).

**`groundHead`:** Produces `{ fact, weight }[]`. Multiple substitutions
yielding the same fact have their weights summed (Z-set addition on the
output relation).

**`distinct`:** Applied after each semi-naive iteration, but only to entries
that were modified during that iteration. The evaluator tracks a **dirty
map** (`Map<string, { fact: Fact, preWeight: number }>`) during each
iteration. The key is `factKey(fact)`. `preWeight` is the fact's weight
*before the first mutation in this stratum evaluation* — recorded on first
touch, never overwritten. After the iteration, for each dirty entry:
weight > 1 → clamp to 1, weight < 0 → clamp to 0 (prune). This is
O(|modified entries|) per iteration, not O(|relation|). Prevents weight
explosion from transitive closure (`fugue_descendant`) and ensures the
binary "present/absent" signal is preserved for negation.

**Delta extraction from the dirty map:** After the stratum converges, the
dirty map contains every fact that was touched. For each entry, compare
`preWeight` (before this stratum evaluation began) to the current weight
in the db (after convergence + `distinct`). If the weight crossed zero:
- `preWeight ≤ 0` and current `> 0` → emit `+1` (new derived fact)
- `preWeight > 0` and current `≤ 0` → emit `−1` (retracted derived fact)

The output is a **delta `Database`** — a `Database` where each relation
contains facts with weight `+1` or `−1`. This type is natural because
`Database` already groups facts by predicate and stores weights per tuple.
The delta `Database` serves both inter-stratum propagation (seed the next
stratum via `db.getRelation(pred).weightedTuples()`) and resolution
extraction (read `winner` and `fugue_before` relations directly). No
`ZSet<Fact>` intermediate or `groupByPredicate` splitting step is needed.

This is O(|touched facts|) — no separate snapshot pass, no diffing the
entire derived database. The dirty map serves double duty: `distinct`
clamping and delta extraction.

### Cross-Time Semi-Naive with Weights

At outer time step t, when `Δ_ground[t]` arrives:

1. Apply `Δ_ground[t]` to the accumulated weighted `Database`:
   for each `(fact, weight)` in the delta, call
   `db.addWeightedFact(fact, weight)`.
2. For each affected stratum (bottom-up):
   a. Seed `currentDelta` from the changed input predicates (facts
      whose weight changed). This is the `_inputDelta` fix.
   b. Semi-naive iterate with weights:
      - For each rule, for each positive atom index, evaluate with that
        atom against `currentDelta`, others against full `db`. Weights
        multiply through joins.
      - `groundHead` produces weighted facts. Sum into a `nextDelta`
        relation.
      - Merge `nextDelta` into accumulated `db` (weight addition).
      - Apply `distinct` to modified entries only (dirty set).
      - New `currentDelta` = entries whose post-`distinct` weight
        differs from pre-iteration weight.
   c. Repeat until convergence (currentDelta is empty).
   d. The stratum's output delta = entries in derived predicates whose
      weight changed from the pre-step value (0→1 or 1→0).
3. Propagate stratum deltas upward to higher strata.
4. Collect output delta: derived facts whose weight changed.

This algorithm is uniform across monotone and negation strata. When a
ground fact with weight −1 arrives:
- Monotone stratum: the −1 propagates through joins, producing −1
  derivations. `distinct` clamps. Facts that lost all support reach
  weight 0 and are retracted.
- Negation stratum: the lower stratum may retract a `superseded` fact.
  The `winner` rule's `not superseded(...)` now succeeds for a
  previously-blocked substitution, producing a +1 winner. The −1 for
  the old winner and +1 for the new winner are the natural output.

### Unified Evaluator: Batch as Degenerate Case

```typescript
createEvaluator(rules) → Evaluator
  .step(deltaFacts: ZSet<Fact>) → StepResult
  .step(deltaFacts: ZSet<Fact>, deltaRules: ZSet<Rule>) → StepResult
  .currentDatabase() → Database
  .currentResolution() → { winners, fuguePairs }
  .reset() → void

evaluate(rules, facts) → Database
  // Convenience: createEvaluator(rules).step(allFactsAsPositive).db
```

The batch pipeline's `solve()` calls `evaluate(rules, facts)` which creates
a fresh evaluator and feeds all facts as +1. The incremental pipeline's
evaluation stage holds a persistent `Evaluator` and calls `step(delta)`.

The "correctness oracle" property is preserved: the batch path creates a
fresh evaluator per call (no accumulated state), so it's independent of
the incremental path's accumulated state. The *implementation* is shared,
but the *state* is separate — which is the right kind of independence for
an oracle.

### Resolution Extraction Without Snapshot-and-Diff

Currently, the incremental evaluator snapshots derived predicates before
and after each step, then diffs to find what changed. With the dirty-map
approach, the delta falls out of `evaluateStratumFromDelta` directly:
each stratum returns a delta `Database` containing facts whose weight
crossed zero during that stratum's evaluation.

At the `step()` level, the evaluator collects per-stratum output delta
`Database`s and merges them. Resolution extraction reads the delta
`Database` directly:
- `winnerFactsToResolution(deltaDb)` reads
  `deltaDb.getRelation('winner').weightedTuples()`.
- `fuguePairFactsToResolution(deltaDb)` reads
  `deltaDb.getRelation('fugue_before').weightedTuples()`.

No `ZSet<Fact>` intermediate is needed. The `groupByPredicate` function
is eliminated entirely — `Database` already *is* facts grouped by
predicate. The `ZSet<Fact>` type does not appear inside the evaluator's
internal data flow. It only appears at the boundary: the `step()` input
(`ZSet<Fact>` from projection) is converted to weighted `Database` entries
on arrival.

**Exception: rule changes.** On rule change, all derived facts are wiped
and all strata replayed. Here we use a lightweight before/after snapshot
scoped to derived predicates only — not the general-case path. Rule
changes are rare (custom constraint addition/retraction) and inherently
O(|DB|), so the snapshot cost is negligible.

`currentResolution()` reads the `winner` and `fugue_before` relations from
the accumulated Database. Since `tuples()` returns weight > 0 tuples, this
works unchanged.

### `Relation` and `Database` Weight Awareness

`Relation.add(tuple)` sums weight +1 via `addWeighted`. Returns true if
the fact was newly present (weight went from ≤0 to >0).
`Relation.has(tuple)` checks weight > 0. `Relation.tuples()` returns
tuples with weight > 0. `Relation.weightedTuples()` returns
`{ tuple, weight }` pairs for weight > 0 entries (used by
`evaluatePositiveAtom` to avoid double serialization).

`Database.addFact(f)` calls `relation.add()`. `Database.addWeightedFact(f, w)`
calls `relation.addWeighted()`. `Database.hasFact(f)` checks weight > 0.

`Relation.remove(tuple)` and `Database.removeFact(f)` delete entries
entirely. `Database.mergeFrom(other)` performs set-union (weight +1 per
tuple). These methods serve the current DRed / snapshot-and-diff code
in `incremental-evaluate.ts` and become dead code in Phase 2 (see
Pre-Phase 2 Note).

### Functional Core / Imperative Shell

The stratum evaluation logic is a pure-ish function: given rules, a mutable
database, and an input delta, run weighted semi-naive to convergence and
return the output delta. This is independently testable:

```typescript
function evaluateStratumFromDelta(
  rules: readonly Rule[],
  db: Database,
  inputDelta: Database,
  hasNegOrAgg: boolean,
): Database  // output delta (facts whose weight crossed zero)
```

`db` is mutated as a side effect of convergence (weight additions, `distinct`
clamping). The return value is a `Database` containing the net delta —
facts with weight `+1` (newly derived) or `−1` (retracted).

**Internals of `evaluateStratumFromDelta`:**

The function maintains a single dirty map
(`Map<string, { fact: Fact, preWeight: number }>`, keyed by `factKey`)
across the entire evaluation (not per-iteration). On first touch of any
fact, the map records the fact's `preWeight` from the db. This pre-weight
is never overwritten — it represents the state before this stratum
evaluation began.

For **positive-only strata** (no negation/aggregation):
1. Initial pass: evaluate all rules against full db. For each derived
   `WeightedFact`, if the fact is new to db (weight ≤ 0), apply it via
   `addWeightedFact` and add to the initial delta `Database`. Record
   `preWeight` in the dirty map on first touch.
2. Semi-naive iteration: use the delta from the previous pass. For each
   rule × positive atom index, evaluate with that atom against delta,
   others against full db. Apply new/changed facts to db and next-delta.
3. After each iteration, apply `distinct` to dirty entries (clamp > 1
   to 1, < 0 to 0).
4. Repeat until convergence (next-delta is empty).

For **negation/aggregation strata**:
1. Naive iteration: evaluate all rules against full db. For each derived
   `WeightedFact` where the fact is new or absent, apply it. Track in
   dirty map.
2. Apply `distinct` after each pass.
3. Repeat until no changes.

After convergence, scan the dirty map: compare each entry's `preWeight`
to the current weight in db. Entries that crossed zero go into the
output delta `Database` with weight `+1` or `−1`.

The `createEvaluator` closure is the imperative shell that holds
accumulated state and orchestrates stratum evaluation across time steps.

**Note on `aggregate.ts`:** `evaluateAggregation` constructs result
substitutions via `new Map(baseSub)` — this becomes
`new Map(baseSub.bindings)`. The output substitution's weight is set to 1
(aggregation is a group-by boundary that resets provenance). This is the
one spot where the `Substitution` type change requires a structural code
change rather than a mechanical `.bindings` insertion.

## Migration Inventory: `incremental-evaluate.ts`

When `incremental-evaluate.ts` is deleted, its functions go to one of
three destinations:

### Migrates to `evaluator.ts` (the new unified evaluator)

| Function | Notes |
|----------|-------|
| `buildPredicateToAffectedStrata` | Stratum dependency analysis — which strata are affected by changed predicates |
| `affectedStrata` | BFS for transitively affected strata |
| `stratumDerivedPredicates` | Returns head predicates for a stratum |
| `winnerFactsToResolution` | Rewritten to take delta `Database`, reads `deltaDb.getRelation('winner').weightedTuples()`. Returns `ZSet<ResolvedWinner>`. |
| `fuguePairFactsToResolution` | Rewritten to take delta `Database`, reads `deltaDb.getRelation('fugue_before').weightedTuples()`. Returns `ZSet<FugueBeforePair>`. |
| `ruleIdentity` / `termId` / `bodyElementId` | Structural identity for rule matching on retraction |
| `stratumHasNegationOrAggregation` | Still needed for aggregation dispatch (aggregation weight semantics are deferred — aggregation strata still get special treatment) |

### Simplified and inlined

| Function | Notes |
|----------|-------|
| `applyFactDelta` | Becomes a one-liner: `zsetForEach(delta, e => db.addWeightedFact(e.element, e.weight))`. Internal to evaluator, not exported. |

### Dead — removed entirely

| Function | Reason |
|----------|--------|
| `groupByPredicate` | Eliminated — delta `Database` already groups by predicate |
| `diffDatabases` | Snapshot-and-diff eliminated |
| `snapshotDerivedPredicates` | Snapshot-and-diff eliminated |
| `evaluateMonotoneStratumIncremental` | Replaced by uniform weighted semi-naive |
| `evaluateStratumDRed` | DRed eliminated |
| `fullRecompute` (closure-internal) | Rule changes use `step` with accumulated ground facts |
| `isReferencedByStratum` | Never called (was always dead) |
| `createIncrementalDatalogEvaluator` | Replaced by `createEvaluator` |

### Dead in other files — removed entirely

| Function | File | Reason |
|----------|------|--------|
| `diffFuguePairs` | `kernel/native-resolution.ts` | Exported but zero call sites |
| `flattenPairs` | `kernel/native-resolution.ts` | Only used by `diffFuguePairs` |

## Phases and Tasks

### Phase 1: Weighted `Relation`, `Database`, and `Substitution` ✅

Add weight field to `Relation`'s internal storage. Redefine `Substitution`
from a raw Map to `{ bindings, weight }`. Update all functions in
`unify.ts`, `evaluate.ts`, and `aggregate.ts` that consume `Substitution`.
Existing API behavior unchanged — all weights are 1 in existing code
paths. 1219 tests pass (1198 existing + 21 new weight tests).

#### `Relation` and `Database`

- 1.1 Change `Relation._map` from `Map<string, FactTuple>` to
  `Map<string, { tuple: FactTuple, weight: number }>`. ✅
- 1.2 Update `add(tuple)` to call `addWeighted(tuple, 1)`. Return true
  if weight went from ≤0 to >0. ✅
- 1.3 Implement `addWeighted(tuple, weight)`: sum weights. Prune
  weight-0 entries. Return the new weight. ✅
- 1.4 Update `has(tuple)` to check weight > 0. ✅
- 1.5 Update `tuples()` to return tuples with weight > 0. ✅
- 1.6 Update `size` to count weight > 0 entries. ✅
- 1.7 Implement `getWeight(tuple): number` — returns 0 if absent. ✅
- 1.8 Update `remove(tuple)` to delete the entry entirely (not
  `addWeighted(tuple, -w)` — see Phase 2 note below). ✅
- 1.9 Update `union`, `difference`, `clone`, `isEmpty` for weight
  awareness. ✅
- 1.10 Add `Database.addWeightedFact(f, weight)`. ✅
- 1.11 Update `Database.removeFact(f)` to zero the weight. ✅
- 1.12 `Database.mergeFrom(other)` left as set-union (adds weight +1
  per tuple). Not weight-summing — see Phase 2 note below. ✅
- 1.13 Update `Database.hasFact(f)` to check weight > 0. ✅
- 1.14 Update `Database.size` to count weight > 0 facts. ✅

#### `Substitution` type change and evaluation core

- 1.15 Redefine `Substitution` in `types.ts` from
  `type Substitution = ReadonlyMap<string, Value>` to
  `interface Substitution { readonly bindings: ReadonlyMap<string, Value>; readonly weight: number; }`.
  The old `Substitution` type alias is removed — one type, one name. ✅
- 1.16 Update `EMPTY_SUBSTITUTION` in `unify.ts` to
  `{ bindings: new Map(), weight: 1 }`. ✅
- 1.17 Update `extendSubstitution` to read `sub.bindings`, return
  `{ bindings: newMap, weight: sub.weight }`. ✅
- 1.18 Update `resolveTerm`: `sub.get(name)` → `sub.bindings.get(name)`. ✅
- 1.19 Update `unifyTermWithValue`: `sub.get(...)` / `sub.has(...)` →
  `sub.bindings.get(...)` / `sub.bindings.has(...)`. ✅
- 1.20 Update `matchAtomWithTuple`: passes `Substitution` through
  (weight preserved from input). ✅
- 1.21 Update `matchAtomAgainstRelation`: accept and return
  `Substitution[]`. ✅
- 1.22 Update `evaluateGuard` / `resolveGuardTerm`: `sub.get(...)` /
  `sub.has(...)` → `sub.bindings.get(...)` / `sub.bindings.has(...)`.
  Return `Substitution | null` (weight preserved). ✅
- 1.23 Update `groundAtom`: `sub.get(...)` / `sub.has(...)` →
  `sub.bindings.get(...)` / `sub.bindings.has(...)`. ✅
- 1.24 Update `evaluatePositiveAtom`: for each sub × tuple match, new
  weight = `sub.weight × relation.getWeight(tuple)`. Uses
  `relation.weightedTuples()` to avoid double serialization. ✅
- 1.25 Update `evaluateNegation`: filter on weight > 0 tuples. Return
  `Substitution[]` (weight preserved on pass). ✅
- 1.26 Update `evaluateGuardElement`: filter. Return `Substitution[]`
  (weight preserved on pass). ✅
- 1.27 Update `evaluateAggregation` in `aggregate.ts`:
  `new Map(baseSub)` → `new Map(baseSub.bindings)`;
  `matched.get(...)` / `matched.has(...)` →
  `matched.bindings.get(...)` / `matched.bindings.has(...)`;
  `sub.get(...)` / `sub.has(...)` in `groupKey`/`groupValues` →
  `sub.bindings.get(...)` / `sub.bindings.has(...)`;
  output substitutions wrapped as `{ bindings: sub, weight: 1 }`. ✅
- 1.28 Update `evaluateAggregationElement`: return `Substitution[]`
  (weight = 1 from aggregation output). ✅
- 1.29 Update `evaluateRule` / `evaluateRuleSemiNaive`: accept/produce
  `Substitution`s. Return `WeightedFact[]`.
  Deduplicate by fact key, summing weights. ✅
- 1.30 Update `groundHead`: accept `Substitution[]`, produce
  `WeightedFact[]`. Sum weights for duplicate facts. ✅
- 1.31 Update batch entry points (`evaluate`, `evaluatePositive`,
  `evaluateNaive`) — all internal calls use `Substitution` with weight 1.
  Return type stays `Database` (derived facts have weight 1 in batch). ✅
- 1.32 Update stratum evaluation (`evaluatePositiveStratum`,
  `evaluateStratumWithNegation`) to use weighted `evaluateRule` /
  `evaluateRuleSemiNaive`. Filter on `wf.weight > 0`. ✅
- 1.33 Update test files: `sub.get('X')` → `sub.bindings.get('X')`,
  `sub.has('X')` → `sub.bindings.has('X')`,
  `sub.size` → `sub.bindings.size` across `unify.test.ts`,
  `aggregate.test.ts`, and any other test files that directly inspect
  substitution contents. ✅
- 1.34 All 1219 tests pass with zero behavioral change. ✅

#### Tests

- Existing `Relation.remove` tests in `incremental-evaluate.test.ts`
  continue to pass.
- Existing `Database.removeFact` tests continue to pass.
- All `evaluate.test.ts`, `unify.test.ts`, `aggregate.test.ts` tests pass
  (with `.bindings` access pattern updates).
- New: `Relation.addWeighted` sums weights correctly.
- New: `Relation.addWeighted` prunes zero-weight entries.
- New: `Relation.getWeight` returns 0 for absent tuples.
- New: `Relation.tuples` excludes weight ≤ 0 entries.
- New: `Database.addWeightedFact` sums weights.
- New: roundtrip: `addWeighted(t, 1)` then `addWeighted(t, -1)` → weight 0,
  `has` returns false, `tuples` excludes it.
- New: weight-1 inputs to `evaluateRule` produce identical facts to
  previous unweighted output (property test over default rules).
- New: weighted positive atom join multiplies weights correctly.
- New: weighted negation filters on weight > 0, not mere presence.
- New: weighted `groundHead` sums weights for duplicate facts.

### Phase 2: Unified Evaluator ✅

Replace both evaluators with one. `evaluate()` becomes a wrapper. DRed,
snapshot-and-diff, and `fullRecompute` are eliminated. Functions from
`incremental-evaluate.ts` migrate here.

Implementation order: build bottom-up from the core stratum evaluator
to the public API, so each layer is testable before the next.

#### 2A: Core — `evaluateStratumFromDelta` and dirty-map infrastructure

- 2.0 Fix `Database.clone()` to preserve weights. Delegates to
  `Relation.clone()` per predicate, which copies the internal map
  directly (including weight ≤ 0 entries). Previously iterated
  `tuples()` and called `add()`, silently flattening weights. ✅
- 2.1 Create `datalog/evaluator.ts`. Dirty-map type:
  `Map<string, { fact: Fact; preWeight: number }>` keyed by `factKey`.
  `touchFact(db, dirty, fact, weightDelta)` records `preWeight` on
  first touch and calls `db.addWeightedFact`. ✅
- 2.2 `applyDistinct(db, dirty)`: clamps dirty entries with weight > 1
  to 1, weight < 0 to 0. Returns true if any weight changed. ✅
- 2.3 `extractDelta(db, dirty)`: scans dirty map, compares `preWeight`
  to current weight. Entries that crossed zero go into a returned delta
  `Database` with weight `+1` or `−1`. ✅
- 2.4 `evaluateStratumFromDelta(rules, db, inputDelta, hasNegOrAgg,
  inputHasRetractions)`: positive strata use weighted semi-naive
  (initial pass + delta iteration); negation/aggregation strata use
  naive iteration. Both paths share dirty map, `applyDistinct`, and
  `extractDelta`. When retractions are present (`inputHasRetractions`),
  both stratum types use wipe-and-recompute for correctness — the
  dirty map captures pre-wipe weights so delta extraction correctly
  produces −1 for retracted facts. Returns output delta `Database`. ✅

  **Implementation note:** `Relation.allWeightedTuples()` and
  `Relation.allEntryCount` were added to support iterating delta
  databases that contain negative-weight entries. `weightedTuples()`
  filters to weight > 0, which is correct for accumulated databases
  but would miss retractions in delta databases.

  **Implementation note on retractions:** The retraction flag propagates
  through strata — if a lower stratum produces −1 deltas, higher strata
  also use wipe-and-recompute. This is conservative but correct; true
  weighted semi-naive retraction propagation (the DBSP ideal) is deferred
  to a future optimization pass.

#### 2B: Shell — `createEvaluator` and `step`

- 2.5 Migrated from `incremental-evaluate.ts`:
  `buildPredicateToAffectedStrata`, `computeAffectedStrata` (renamed
  from `affectedStrata`), `stratumDerivedPredicates`,
  `stratumHasNegationOrAggregation`, `ruleIdentity` / `termId` /
  `bodyElementId`. `winnerFactsToResolution` and
  `fuguePairFactsToResolution` rewritten to take a delta `Database` —
  they read `deltaDb.getRelation('winner').allWeightedTuples()` and
  `deltaDb.getRelation('fugue_before').allWeightedTuples()`.
  `groupByPredicate` not migrated (dead). ✅
- 2.6 `createEvaluator(rules)` with mutable state: accumulated
  `Database`, rules, strata, `predToStrata`, `allDerivedPreds`,
  `accumulatedGroundFacts` (Map<string, Fact> for rule-change replay). ✅
- 2.7 `step(deltaFacts, deltaRules)` — no-rule-change path:
  applies weighted delta to db, computes affected strata, builds
  per-stratum input delta, calls `evaluateStratumFromDelta` for each
  affected stratum bottom-up, propagates stratum output deltas upward
  (each stratum's output delta `Database` feeds the next stratum's
  `inputDelta`), collects the final output delta, extracts resolution
  via `winnerFactsToResolution` / `fuguePairFactsToResolution`. ✅
- 2.8 `step` — rule-change path: restratify, snapshot derived facts
  (scoped to union of old + new derived preds), wipe derived facts,
  replay all strata from scratch, diff pre/post snapshots. ✅
- 2.9 `currentDatabase()`, `currentResolution()`, `reset()`. ✅

#### 2C: Batch wrappers

- 2.10 `evaluateUnified(rules, facts)` as convenience wrapper:
  validates stratification, creates fresh evaluator, steps with all
  facts as +1, returns database. Named `evaluateUnified` (not
  `evaluate`) to coexist with the old `evaluate` during Phase 2.
  Phase 3 will rename and rewire imports. ✅
- 2.11 `evaluatePositiveUnified(rules, facts)` as alias. Same naming
  rationale as 2.10. ✅
- 2.12 `evaluateNaive(rules, facts)` remains in `evaluate.ts` as a
  test utility (unchanged). ✅

#### 2D: Verification

- 2.13 `evaluateUnified()` produces identical results to the old batch
  evaluator: verified via comprehensive equivalence tests including
  simple positive rules, LWW rules, stratified negation, guard
  conditions, multiple rules per predicate, and full default LWW +
  Fugue rules. All 1219 existing tests still pass. ✅
- 2.14 Incremental path (`createEvaluator` + `step`) produces identical
  results to the old incremental evaluator: verified via equivalent
  tests for LWW resolution, supersession, retraction, permutation,
  transitive closure, Fugue rules, negation, rule changes, and
  batch equivalence. 54 new tests in `evaluator.test.ts`. ✅

#### Tests

- All existing `evaluate.test.ts` tests pass via the new `evaluate()`.
- All existing `incremental-evaluate.test.ts` tests pass via the new
  `createEvaluator` + `step`.
- New: `evaluateStratumFromDelta` unit tests — pure function, testable
  in isolation with small rule sets and deltas. Verify that `db` is in
  a consistent state after the call (weights clamped, no stale dirty
  entries).
- New: `extractDelta` unit tests — verify zero-crossing detection from
  dirty map entries with known preWeight values. Verify output is a
  `Database` with ±1 weights grouped by predicate.
- New: property test — for any insertion sequence, `reset() + replay all`
  equals accumulated state.
- New: retraction through negation stratum without DRed — insert two
  competing values, retract the winner's ground fact, verify new winner
  emerges via weight propagation (not recomputation).
- New: transitive closure with `distinct` — verify `fugue_descendant`
  weights stay at 0/1 regardless of path count.
- New: three-way oracle — batch `evaluate()`, fresh `createEvaluator +
  step(all)`, and accumulated `createEvaluator + step(one-at-a-time)` all
  produce the same database.

### Phase 3: Wire Unified Evaluator into Pipeline 🔴

Replace `incremental-evaluate.ts` usage in the evaluation stage with the
unified evaluator. Update batch pipeline.

- 3.1 Update `kernel/incremental/evaluation.ts` to use `createEvaluator`
  instead of `createIncrementalDatalogEvaluator`. 🔴
- 3.2 Update `kernel/pipeline.ts` to use `evaluate()` from the unified
  module (`datalog/evaluator.ts`). 🔴
- 3.3 Verify the evaluation stage's strategy switching (native ↔ Datalog)
  works with the new evaluator. 🔴
- 3.4 Verify all 42 pipeline differential tests pass. 🔴
- 3.5 Verify all 30 integration tests pass. 🔴

#### Tests

- All existing pipeline and integration tests pass.
- New: pipeline differential test with retraction — insert value, retract
  it, verify incremental pipeline matches batch.

### Phase 4: Cleanup and Documentation 🔴

Remove dead code, update documentation, update exports.

- 4.1 Delete `datalog/incremental-evaluate.ts` (all live functions migrated
  in Phase 2; all dead functions identified in Migration Inventory). 🔴
- 4.2 Remove from `evaluate.ts`: `evaluatePositiveStratum`,
  `evaluateStratumWithNegation` (stratum-level logic now lives in the
  unified evaluator). The rule-level functions (`evaluateRule`,
  `evaluateRuleSemiNaive`, `evaluatePositiveAtom`, `evaluateNegation`,
  `evaluateGuardElement`, `evaluateAggregationElement`, `groundHead`,
  `getPositiveAtomIndices`) remain — they are the evaluation core. 🔴
- 4.3 Remove from `evaluate.ts`: `evaluate` and `evaluatePositive`
  (moved to `evaluator.ts` in Phase 2). Keep `evaluateNaive` as a test
  utility. 🔴
- 4.4 Remove from `kernel/native-resolution.ts`: `diffFuguePairs`,
  `flattenPairs` (zero call sites). 🔴
- 4.5 Update `datalog/index.ts` exports: remove
  `createIncrementalDatalogEvaluator`, `applyFactDelta`, `diffDatabases`,
  `groupByPredicate`, `evaluate` (from evaluate.ts), `evaluatePositive`
  (from evaluate.ts). Add `createEvaluator`, `evaluate` (from
  evaluator.ts), `evaluatePositive` (from evaluator.ts).
  `groupByPredicate` has no consumers — it was used only for the
  `ZSet<Fact>` → per-predicate split that `Database` now replaces. 🔴
- 4.6 Update `kernel/index.ts` exports: remove `diffFuguePairs`. 🔴
- 4.7 Update `src/index.ts` barrel exports. 🔴
- 4.8 Update TECHNICAL.md: document weighted relations, unified evaluator,
  removal of DRed, the `distinct` operator, enriched `Substitution` type.
  Rewrite the Datalog Evaluator section. 🔴
- 4.9 Update LEARNINGS.md with new entries. 🔴
- 4.10 Update theory/incremental.md §9.4 to reflect that provenance is now
  implemented via Z-set weights with `distinct`. 🔴
- 4.11 Remove the `_inputDelta` prep note from
  `.plans/004-incremental-roadmap.md` Plan 008 section (resolved). 🔴
- 4.12 Verify all tests pass, zero diagnostics. 🔴

#### Tests

- Full test suite passes: target ≥ 1219 tests (some DRed-specific
  tests may be replaced by weight-propagation tests).

## Transitive Effect Analysis

### `Relation` internal storage change

Every consumer of `Relation` is affected:
- `Database` — delegates to `Relation`. Updated in Phase 1.
- `evaluate.ts` — calls `relation.tuples()`, `db.hasFact()`,
  `db.addFact()`, `db.mergeFrom()`. All backward compatible after Phase 1.
  Weighted rule evaluation uses `relation.getWeight()` for weight
  multiplication.
- `incremental-evaluate.ts` — calls `relation.tuples()`, `db.addFact()`,
  `db.removeFact()`, `db.mergeFrom()`. Subsumed in Phase 2.
- `aggregate.ts` — calls `db.getRelation(pred).tuples()`. Backward
  compatible (`tuples()` returns weight > 0 tuples).
- `unify.ts` — operates on enriched `Substitution` after Phase 1.
  Matching functions access `sub.bindings` and pass weight through
  unchanged.
- `resolve.ts` — calls `db.getRelation(pred).tuples()`. Backward
  compatible.
- `kernel/projection.ts` (batch) — creates `Fact` objects, not
  `Relation`. No change.
- Test files — create `Relation`/`Database` via `add`/`addFact`. Backward
  compatible.

### `Substitution` type redefinition

`Substitution` changes from `ReadonlyMap<string, Value>` to
`{ bindings: ReadonlyMap<string, Value>, weight: number }`. The name
stays the same — no `WeightedSub`, no alias, no parallel type. This
affects:
- `unify.ts` — `EMPTY_SUBSTITUTION`, `extendSubstitution`, `resolveTerm`,
  `unifyTermWithValue`, `matchAtomWithTuple`, `matchAtomAgainstRelation`,
  `evaluateGuard`, `resolveGuardTerm`, `groundAtom`. All updated in
  Phase 1. Access pattern: `sub.get(...)` → `sub.bindings.get(...)`.
- `evaluate.ts` — all body element evaluation functions, `groundHead`.
  Updated in Phase 1.
- `aggregate.ts` — `evaluateAggregation`, `evaluateAggregationForSubs`,
  `groupKey`, `groupValues`. Updated in Phase 1. `new Map(baseSub)` →
  `new Map(baseSub.bindings)`. Output weight = 1.
- Test files — `unify.test.ts` (~8 sites), `aggregate.test.ts` (~20+
  sites) need `sub.get(...)` → `sub.bindings.get(...)` updates.
  `EMPTY_SUBSTITUTION` import is unchanged (same name, richer type).

### Removal of `incremental-evaluate.ts`

Consumers:
- `kernel/incremental/evaluation.ts` — imports
  `createIncrementalDatalogEvaluator`. Updated in Phase 3 to use
  `createEvaluator`.
- `datalog/index.ts` — re-exports. Updated in Phase 4.
- `tests/datalog/incremental-evaluate.test.ts` — tests rewritten to use
  `createEvaluator`. The test *intent* is preserved; the API changes.

### Removal of `diffFuguePairs` / `flattenPairs`

Consumers:
- `kernel/index.ts` — exports. Updated in Phase 4.
- `src/index.ts` — re-exports. Updated in Phase 4.
- Zero call sites in production code or tests. Safe removal.

### Removal of `diffDatabases`

Consumers:
- `incremental-evaluate.ts` — the only production consumer. Deleted.
- `tests/datalog/incremental-evaluate.test.ts` — tested directly.
  Tests removed or rewritten.
- `datalog/index.ts` — re-exports. Updated.

### `evaluate()` import path change

`evaluate()` moves from `datalog/evaluate.ts` to `datalog/evaluator.ts`.
Consumers:
- `kernel/pipeline.ts` — calls `evaluate(rules, facts)`. Updated import
  path in Phase 3.
- Test files — `evaluate.test.ts`, `rules.test.ts`, `aggregate.test.ts`,
  `resolve.test.ts`, `lww-equivalence.test.ts`, `fugue-equivalence.test.ts`.
  Updated import paths.

### `winnerFactsToResolution` anti-cancellation workaround

With proper weight propagation, a winner change produces a derived fact
delta where the old winner's weight goes from 1 to 0 (delta −1) and the
new winner's weight goes from 0 to 1 (delta +1). These are different
facts (different CnId, content) with different keys. Under `zsetMap`
keyed by slotId, they would still collide. The anti-cancellation
workaround (group by slotId, apply replacement semantics) remains
necessary — it's a key-collision issue at the resolution extraction layer,
not a weight-propagation issue. No change needed.

## Testing Strategy

### Three-Way Oracle

For every insertion sequence, verify:
1. `evaluate(rules, allFacts)` — batch (fresh evaluator, all facts at once)
2. `createEvaluator(rules) + step(allFacts)` — incremental, one big step
3. `createEvaluator(rules) + step(fact₁) + step(fact₂) + ...` — incremental,
   one fact per step

All three must produce the same `Database`. This replaces the old
batch-vs-incremental oracle with a stronger property.

### Weight-Specific Tests

- Weight propagation through joins (positive atom × positive atom).
- Weight propagation through negation (filter on weight > 0).
- `distinct` clamping prevents weight explosion on transitive closure.
- Retraction cascade: ground fact −1 → derived fact weight drops to 0.
- Re-derivation: retracted fact has alternative support → weight stays 1.

### Stratum Evaluation Unit Tests

`evaluateStratumFromDelta` is a pure(ish) function — testable in isolation
with small rule sets and hand-crafted deltas. Tests should cover:
- Positive-only rules with +1 delta (new derivations).
- Positive-only rules with −1 delta (retraction propagation).
- Negation rules with delta that changes negation outcome.
- Recursive rules (transitive closure) with `distinct` convergence.

### Permutation Tests

Existing permutation tests (3 values in all orderings produce same
resolution) continue to work and also validate weight consistency.

### Custom Rule Tests

Existing custom rule tests (reversed LWW, rule addition/retraction)
continue to work via the unified evaluator.

## Resources for Implementation

### Files to Read

- `src/datalog/types.ts` — `Relation`, `Database`, `Substitution`, `Fact`
- `src/datalog/evaluate.ts` — `evaluateRule`, `evaluateRuleSemiNaive`,
  `evaluatePositiveAtom`, `evaluateNegation`, `groundHead`,
  `getPositiveAtomIndices`
- `src/datalog/unify.ts` — `matchAtomAgainstRelation`,
  `matchAtomWithTuple`, `EMPTY_SUBSTITUTION`, `extendSubstitution`,
  `resolveTerm`, `evaluateGuard`
- `src/datalog/incremental-evaluate.ts` — the code being replaced;
  understand `step()`, `winnerFactsToResolution`,
  `fuguePairFactsToResolution`, `affectedStrata`,
  `buildPredicateToAffectedStrata`, `groupByPredicate`, `ruleIdentity`
- `src/datalog/stratify.ts` — `stratify`, `Stratum`, `bodyPredicates`,
  `headPredicates`
- `src/datalog/aggregate.ts` — `evaluateAggregation`
- `src/kernel/incremental/evaluation.ts` — the consumer being updated
- `src/kernel/pipeline.ts` — the batch consumer being updated
- `src/base/zset.ts` — `ZSet<T>` type and algebra
- `theory/incremental.md` §9 — the theoretical foundation
- `LEARNINGS.md` — Plan 006 learnings, especially "DRed for Negation
  Strata" and "`zsetMap` with Shared Output Keys"

### Files to Modify

- `src/datalog/types.ts` — `Relation`, `Database`, `Substitution`
- `src/datalog/evaluate.ts` — weighted evaluation core, remove stratum
  functions, remove `evaluate`/`evaluatePositive` (moved to evaluator.ts)
- `src/datalog/unify.ts` — `Substitution` `.bindings` access pattern
- `src/datalog/aggregate.ts` — `Substitution` `.bindings` access pattern,
  `new Map(baseSub)` → `new Map(baseSub.bindings)`
- `src/datalog/evaluator.ts` — new unified evaluator (new file)
- `src/datalog/index.ts` — updated exports
- `src/kernel/incremental/evaluation.ts` — use new evaluator
- `src/kernel/pipeline.ts` — import path change
- `src/kernel/index.ts` — remove dead exports
- `src/kernel/native-resolution.ts` — remove `diffFuguePairs`, `flattenPairs`
- `src/index.ts` — updated barrel exports

### Files to Delete

- `src/datalog/incremental-evaluate.ts` — subsumed by `evaluator.ts`

### Test Files to Modify

- `tests/datalog/evaluate.test.ts` — import path updates for `evaluate`,
  `evaluatePositive`
- `tests/datalog/incremental-evaluate.test.ts` — rewrite for new API;
  `Relation`/`Database` weight tests remain; evaluator tests use
  `createEvaluator`
- `tests/datalog/unify.test.ts` — `sub.get(...)` → `sub.bindings.get(...)`,
  `sub.has(...)` → `sub.bindings.has(...)`, `sub.size` →
  `sub.bindings.size` (~8 sites)
- `tests/datalog/aggregate.test.ts` — `results[0]!.get(...)` →
  `results[0]!.bindings.get(...)` (~20+ sites), import path updates
- `tests/datalog/rules.test.ts` — import path updates
- `tests/kernel/resolve.test.ts` — import path updates
- `tests/solver/lww-equivalence.test.ts` — import path updates
- `tests/solver/fugue-equivalence.test.ts` — import path updates
- `tests/kernel/incremental/evaluation.test.ts` — import updates for
  new evaluator

## Alternatives Considered

### Keep DRed, Only Fix `_inputDelta`

The minimal fix: use the input delta to seed semi-naive in
`evaluateMonotoneStratumIncremental`. This makes monotone strata O(|Δ|)
but leaves negation strata at O(|stratum|). Rejected because: (a) it
perpetuates the DRed/snapshot-and-diff complexity, (b) it doesn't help
negation strata, (c) Plan 008 queries may have large negation strata,
and (d) we'd still have two evaluator implementations.

### Create Parallel Weighted Function Variants

Create `evaluateRuleWeighted`, `evaluatePositiveAtomWeighted`, etc.
alongside the existing unweighted versions, with unweighted versions
becoming thin wrappers. Rejected because: (a) 10+ functions where the
weighted version IS the real implementation and the wrapper strips weights
— pure boilerplate, (b) the wrapper and real function drift if anyone
edits the wrapper, (c) redefining `Substitution` to carry a weight
eliminates the need for parallel variants entirely.

### Add Weights Only to a New `WeightedRelation` Type

Plan 006 initially considered this (see LEARNINGS.md "ZSet<Fact> Is the
Right Unification Point"). Rejected because: (a) conversion between
`Relation` and `WeightedRelation` at every boundary is pure overhead,
(b) the batch evaluator's inner functions take `Database` parameters —
a separate type requires adapters, (c) one type with an extra field is
simpler than two isomorphic types.

### Use `ZSet<Fact>` Internally Instead of Weighted `Relation`

Replace `Database` with `Map<string, ZSet<Fact>>`. Rejected in Plan 006
because the semi-naive loop mutates `db` between iterations. This
concern persists — `ZSet<T>` is `ReadonlyMap` by design (immutable for
algebraic safety). `Relation` is mutable (mutated by `mergeFrom` between
iterations). The right boundary: `ZSet` for inter-stage deltas
(immutable), weighted `Relation` for intra-evaluator state (mutable).

### Apply `distinct` to Entire Relation After Each Iteration

Walk all entries in a relation to clamp weights. O(|relation|) per
iteration. Rejected in favor of scoped `distinct` with a dirty set:
track which entries were modified during the iteration, clamp only those.
O(|modified entries|) per iteration.

### Full Provenance Tracking (Derivation DAGs per Fact)

Track which specific ground facts support each derivation, enabling
precise retraction. DBSP subsumes this via Z-set arithmetic — weights
encode multiplicity, which is sufficient for determining when a fact
loses all support. Per-fact DAGs are more expensive (memory and
bookkeeping) and unnecessary when `distinct` keeps weights at 0/1.
Rejected in favor of the lighter Z-set weight approach.

### Defer to Plan 008

Address the evaluator during query layer implementation. Rejected because:
(a) Plan 008 would inherit duplicated evaluator code and need to add
weights in two places, (b) the DRed/snapshot-and-diff complexity is
carrying cost in every future plan that touches the evaluator, (c) the
unification is independently valuable for code health.